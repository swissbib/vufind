<?php if (method_exists($this->driver, 'setUseMostSpecificFormat')) {
  $this->driver->setUseMostSpecificFormat(true);
} ?>
<?php foreach ($this->driver->getFormats() as $format): ?>
  <span class="iconlabel <?= $this->record($this->driver)->getFormatClass($format) ?>">
    <?= $this->zendTranslate($format, 'formats') ?>
  </span>
<?php endforeach; ?>
