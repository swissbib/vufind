<? $availableReferences = $this->driver->getSameAs(); ?>
<? if (is_array($availableReferences) && count($availableReferences) > 0): ?>
    <h1><?= $this->translate('page.detail.person.more') ?></h1>
    <ul>
        <? foreach ($availableReferences as $link): ?>
        <?= $this->essubject()->getRecordReference('<li><a href="%s">%s</a></li>', $link, $this->references->record->references) ?>
        <? endforeach; ?>
    </ul>
<? endif; ?>
