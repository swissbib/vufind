<? $links = $this->driver->getSameAs(); ?>
<? if (isset($links)): ?>
    <h1>{reference-link-list-title}</h1>
    <ul>
        <? foreach ($links as $link):  ?>
            <li>
                <a href="<?= $link ?>">
                    <?= $link ?>
                </a>
            </li>
        <? endforeach; ?>
    </ul>
<? endif; ?>

