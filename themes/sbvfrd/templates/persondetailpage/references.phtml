<? $configuredReferences = $this->references->record->references ?>
<? $template = '/detailpage/common/reference.phtml' ?>
<? if ($this->esperson()->hasMatchingRecordReferences($configuredReferences)): ?>
<div class="record-references <?= $this->visibility ?>">
    <h1 class="title"><?= $this->translate('page.detail.person.more') ?></h1>
    <div class="list">
        <ul id="detailpage-person-references">
            <? foreach ($this->esperson()->getSameAs() as $link): ?>
            <li><?= $this->render($template, $this->esperson()->getRecordReference($link, $configuredReferences)) ?></li>
            <? endforeach; ?>
        </ul>
    </div>
</div>
<? endif; ?>
