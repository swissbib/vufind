<? if ($this->esperson()->hasAbstract()): ?>
    <? $abstractInfo = $this->esperson()->getAbstractInfo(false, 120, 2000); ?>
    <div class="row">
        <div class="col-sm-2"><?= $abstractInfo->label ?></div>
        <div class="col-sm-10">
            <? if ($abstractInfo->truncated): ?>
                <span class="abstract-text overflow-hidden"><?= $abstractInfo->text ?></span>
                <?= $this->render('/persondetailpage/common/metadata/abstract-overflow.phtml', ['overflow' => $abstractInfo->overflow]) ?>
            <? else: ?>
                <span class="abstract-text"><?= $abstractInfo->text ?></span>
            <? endif; ?>
        </div>
    </div>
<? endif; ?>
