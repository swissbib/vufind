<? $template = '/detailpage/common/anchor.phtml' ?>
<div class="page-anchors">
    <h1 class="title"><?= $this->translate('page.detail.anchors.title') ?></h1>
    <button class="accordion-collapse hidden-md hidden-lg" data-toggle="collapse" data-target="#detailpage-person-anchors"></button>
    <div class="list">
        <ul id="detailpage-person-anchors">
            <li><?= $this->render($template, ['name' => 'common', 'label' => $this->esperson()->resolveLabelWithDisplayName('page.detail.person.common')]) ?></li>
            <li><?= $this->render($template, ['name' => 'medias', 'label' => $this->esperson()->resolveLabelWithDisplayName('page.detail.person.media.list.title')]) ?></li>
            <? if ($this->coContributorsTotal > 0): ?>
            <li><?= $this->render($template, ['name' => 'coauthors', 'label' => $this->esperson()->resolveLabelWithDisplayName('page.detail.authors.coauthors')]) ?></li>
            <? endif; ?>
            <? if ($this->authorsOfSameGenreTotal > 0): ?>
            <li><?= $this->render($template, ['name' => 'genres', 'label' => $this->translate('page.detail.authors.genres')]) ?></li>
            <? endif; ?>
            <? if ($this->personsOfSameMovementTotal > 0): ?>
            <li><?= $this->render($template, ['name' => 'movements', 'label' => $this->translate('page.detail.authors.movements')]) ?></li>
            <? endif; ?>
            <? if (isset($subjects) && count($subjects) > 0): ?>
            <li><?= $this->render($template, ['name' => 'subjects', 'label' => $this->esperson()->resolveLabelWithDisplayName('page.detail.person.subjects.related')]) ?></li>
            <? endif; ?>
        </ul>
    </div>
</div>
