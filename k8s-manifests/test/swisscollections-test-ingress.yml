apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  namespace: memobase
  name: swisscollections-test-ingress
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  tls:
    - hosts:
        - test.swisscollections.memobase.k8s.unibas.ch
      secretName: test.swisscollections.memobase.k8s.unibas.ch-tls
  rules:
    - host: test.swisscollections.memobase.k8s.unibas.ch
      http:
        paths:
          - path: /
            backend:
              serviceName: swisscollections-test-nginx-service
              servicePort: 80