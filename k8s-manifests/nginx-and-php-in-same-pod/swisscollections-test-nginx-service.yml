apiVersion: v1
kind: Service
metadata:
  namespace: memobase
  name: swisscollections2-test-frontend-service
  labels:
    app: swisscollections2-test-nginx-app
spec:
  ports:
    - port: 80
  selector:
    app: swisscollections2-test-nginx-app
    tier: web
  clusterIP: None