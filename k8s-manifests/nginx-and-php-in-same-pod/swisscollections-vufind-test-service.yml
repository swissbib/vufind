apiVersion: v1
kind: Service
metadata:
  namespace: memobase
  name: swisscollections-vufind-test-service
  labels:
    app: swisscollections-vufind-test-app
spec:
  ports:
    - port: 80
  selector:
    app: swisscollections-vufind-test-app
    tier: web
  clusterIP: None